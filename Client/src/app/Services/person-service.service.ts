import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
import { Person } from '../Models/person';
import { personsData } from '../Models/Persons';

@Injectable({
  providedIn: 'root'
})
export class PersonService {


  constructor() {
   
  }

  getAll() {
    
  }

  add(person: Person) {
   
  }

  edit(person: Person) {
   
  }

  remove(id: number) {
   
   
  }
}
